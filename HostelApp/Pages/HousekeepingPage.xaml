<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HostelApp.Pages.HousekeepingPage"
             Title="Уборка и статусы номеров">
  <StackLayout Spacing="10">
    <SearchBar x:Name="SearchBar" Placeholder="Поиск: номер/тип/статус" />
    <ListView x:Name="RoomsList" HasUnevenRows="True" SeparatorVisibility="None" SelectionMode="None">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <Frame>
              <StackLayout Spacing="6">
                <Label Text="Комната" />
                <Label Text="{Binding Number}" FontAttributes="Bold" />
                <Label Text="{Binding Type}" />
                <Label Text="{Binding StatusRu}" />
                <Grid ColumnSpacing="8" RowSpacing="8">
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                  </Grid.RowDefinitions>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                  </Grid.ColumnDefinitions>
                  <Button Grid.Row="0" Grid.Column="0" Text="Готов" Clicked="OnSetReady" />
                  <Button Grid.Row="0" Grid.Column="1" Text="Уборка" Clicked="OnSetCleaning" />
                  <Button Grid.Row="1" Grid.Column="0" Text="Не беспокоить" Clicked="OnSetDnd" />
                  <Button Grid.Row="1" Grid.Column="1" Text="Ремонт" Clicked="OnSetRepair" />
                </Grid>
              </StackLayout>
            </Frame>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
</ContentPage>
